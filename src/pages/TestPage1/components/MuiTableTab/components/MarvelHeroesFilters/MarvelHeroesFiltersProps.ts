// import type { IMarvelHeroesData } from 'src/interfaces';

export interface MarvelHeroesFiltersProps {
  /**
   * Disable possibility to open Filters Modal.
   * @default false
   */
  isDisabled?: boolean;
}